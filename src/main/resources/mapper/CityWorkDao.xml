<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sgg.covid19.dao.CityWorkDao">
    <select id="queryCityWork" resultType="com.sgg.covid19.entity.CityWork" parameterType="String">
        SELECT date, city, rate
        FROM tb_city_work
    </select>
</mapper>
